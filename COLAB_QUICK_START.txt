==============================================
COLAB QUICK START - Copy & Paste These
==============================================

# 1. Mount Drive
from google.colab import drive
drive.mount('/content/drive')

# 2. Go to repo
import os
os.chdir('/content/2026-Prototype')

# 3. Train T5 with all fixes (RECOMMENDED)
!python scripts/train_maxsight.py \
    --data-dir /content/drive/MyDrive/MaxSight_Training \
    --image-dir /content/drive/MyDrive/MaxSight_Training \
    --batch-size 8 \
    --epochs 20 \
    --use-gradnorm \
    --checkpoint-dir /content/drive/MyDrive/MaxSight/checkpoints \
    --device cuda

==============================================
What's Enabled by Default:
==============================================
✅ EMA (decay=0.9999) - better model quality
✅ Gradient clipping (norm=5.0) - stable training
✅ All 8 T5 training fixes - production ready
✅ Early stopping (patience=10) - auto stops
✅ Validation metrics - mAP, precision, recall
✅ Checkpoint saving - best_model.pt, last_checkpoint.pt

📝 FP32 only (no mixed precision) for stability

==============================================
Common Adjustments:
==============================================

# If OOM (out of memory):
--batch-size 4 --grad-accumulation-steps 8

# For faster training:
--compile --num-workers 4

# To resume from checkpoint:
--resume-from /content/drive/MyDrive/MaxSight/checkpoints/last_checkpoint.pt

# For fine-tuning with lower LR:
--learning-rate 5e-5

==============================================
Monitor Training:
==============================================

# Watch logs
!tail -f /content/drive/MyDrive/MaxSight/checkpoints/training.log

# Check GPU
!nvidia-smi

# View GradNorm weights (logged every 500 steps)
!grep "GradNorm weights" /content/drive/MyDrive/MaxSight/checkpoints/training.log | tail -5

==============================================
All flags: COLAB_TRAINING_COMMANDS.md
==============================================
