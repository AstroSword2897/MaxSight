# Colab-compatible requirements
# Colab has pre-installed packages; we pin versions to avoid conflicts

# Core ML (Colab has torch 2.10.0+)
torch>=2.9.1
torchvision>=0.24.1
torchaudio>=2.9.1

# Data (pin to Colab-compatible versions)
numpy<2.2.0,>=1.26.0  # tensorflow requires <2.2.0
pandas<3.0.0,>=2.2.2  # Colab/gradio require <3.0
pillow<12.0,>=8.0     # gradio requires <12.0

# Other core deps
opencv-python>=4.8.0
scipy>=1.11.0
scikit-learn>=1.3.0
pytest>=9.0.1
optuna>=3.0.0
torchao>=0.14.1
matplotlib>=3.10.7
tqdm>=4.66.0
flask>=3.0.0
flask-cors>=4.0.0

# Optional (install separately if needed)
# librosa>=0.10.0
# sentence-transformers>=2.0.0
# coremltools
# psutil
# faiss-cpu
